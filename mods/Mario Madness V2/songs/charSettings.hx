// that for changing gameovers icons offsets and etc - Mlaofmd

function update(){
    switch(PlayState.instance.dad.curCharacter){
        case 'luigi-toolate':
            icoP2.offset.set(0, 30);
        }
}